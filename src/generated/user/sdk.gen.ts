// This file is auto-generated by @hey-api/openapi-ts

import type { Client, ClientMeta, Options as Options2, RequestResult, TDataShape } from './client';
import { client } from './client.gen';
import type { CreateAuthMappingData, CreateAuthMappingErrors, CreateAuthMappingResponses, CreateUserData, CreateUserErrors, CreateUserResponses, CreateWorkspaceData, CreateWorkspaceErrors, CreateWorkspaceResponses, DeleteAuthMappingData, DeleteAuthMappingErrors, DeleteAuthMappingResponses, DeleteUserData, DeleteUserErrors, DeleteUserResponses, GetAuthMappingsData, GetAuthMappingsErrors, GetAuthMappingsResponses, GetOrgData, GetOrgErrors, GetOrgResponses, GetUserData, GetUserErrors, GetUserResponses, GetWorkspacesData, GetWorkspacesErrors, GetWorkspacesResponses, OpenWebSocketSessionData, OpenWebSocketSessionErrors, OpenWebSocketSessionResponses, QueryUsersData, QueryUsersErrors, QueryUsersResponses, UpdateAuthMappingData, UpdateAuthMappingErrors, UpdateAuthMappingResponses, UpdateUserData, UpdateUserErrors, UpdateUserResponses, UpdateWorkspaceData, UpdateWorkspaceErrors, UpdateWorkspaceResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean, TResponse = unknown> = Options2<TData, ThrowOnError, TResponse> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: keyof ClientMeta extends never ? Record<string, unknown> : ClientMeta;
};

/**
 * Query user accounts
 *
 * Query user accounts using the Dynamic LINQ query language to specify
 * filters, for example to look up accounts before assigning them to
 * workspaces via auth-mappings, to populate user selection lists in
 * administration UIs, or to audit which accounts exist in the
 * organization. An empty request body returns all user accounts. Results
 * are paginated; use the <b>continuationToken</b> from the response to
 * retrieve subsequent pages.
 *
 */
export const queryUsers = <ThrowOnError extends boolean = false>(options?: Options<QueryUsersData, ThrowOnError>): RequestResult<QueryUsersResponses, QueryUsersErrors, ThrowOnError> => (options?.client ?? client).post<QueryUsersResponses, QueryUsersErrors, ThrowOnError>({
    security: [{ name: 'x-ni-api-key', type: 'apiKey' }],
    url: '/users/query',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options?.headers
    }
});

/**
 * Create a user account
 *
 * Create a new user account in the caller's organization to pre-provision
 * policies or metadata before a person first signs in, or to create a
 * service account for machine-to-machine integrations such as automated
 * test execution or data ingestion pipelines. The account type defaults to
 * 'user' if not specified. Service accounts (type 'service') cannot have
 * email, phone, niuaId, or login fields set. In most deployments, human
 * user accounts are created automatically on first sign-in via an identity
 * provider, so explicit creation is typically reserved for service
 * accounts or advance provisioning.
 *
 */
export const createUser = <ThrowOnError extends boolean = false>(options?: Options<CreateUserData, ThrowOnError>): RequestResult<CreateUserResponses, CreateUserErrors, ThrowOnError> => (options?.client ?? client).post<CreateUserResponses, CreateUserErrors, ThrowOnError>({
    security: [{ name: 'x-ni-api-key', type: 'apiKey' }],
    url: '/users',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options?.headers
    }
});

/**
 * Delete a user account
 *
 * Delete a user account by ID. The user's API keys will be invalidated
 * but are not automatically cleaned up, and auth-mappings referencing the
 * user are not removed.
 *
 * Deleting a user orphans any records in this or other services that store
 * the user's ID. Orphaned records will no longer be able to resolve the
 * user's display name, so historical data will lose attribution. In most
 * cases, it is preferable to revoke policies or auth-mappings to restrict
 * access rather than deleting the account.
 *
 */
export const deleteUser = <ThrowOnError extends boolean = false>(options: Options<DeleteUserData, ThrowOnError>): RequestResult<DeleteUserResponses, DeleteUserErrors, ThrowOnError> => (options.client ?? client).delete<DeleteUserResponses, DeleteUserErrors, ThrowOnError>({
    security: [{ name: 'x-ni-api-key', type: 'apiKey' }],
    url: '/users/{id}',
    ...options
});

/**
 * Get a user account by ID
 *
 * Returns the full user account record for the specified user ID,
 * including profile details, directly assigned policies, and account
 * status. Useful for displaying account information in a UI or verifying
 * the account's current state before performing an administrative action.
 *
 */
export const getUser = <ThrowOnError extends boolean = false>(options: Options<GetUserData, ThrowOnError>): RequestResult<GetUserResponses, GetUserErrors, ThrowOnError> => (options.client ?? client).get<GetUserResponses, GetUserErrors, ThrowOnError>({
    security: [{ name: 'x-ni-api-key', type: 'apiKey' }],
    url: '/users/{id}',
    ...options
});

/**
 * Update a user account
 *
 * Update one or more fields on an existing user account to correct profile
 * information, assign or revoke org-level policies, or add keywords and
 * properties for organizational tagging. Only the fields included in the
 * request body are modified; omitted fields remain unchanged.
 *
 * Note: For accounts with <b>type == "user"</b>, the <b>firstName</b>, <b>lastName</b>,
 * <b>email</b>, and <b>login</b> fields are automatically managed by the application
 * and will be overwritten with values from the identity provider the next
 * time the user logs in.
 *
 */
export const updateUser = <ThrowOnError extends boolean = false>(options: Options<UpdateUserData, ThrowOnError>): RequestResult<UpdateUserResponses, UpdateUserErrors, ThrowOnError> => (options.client ?? client).put<UpdateUserResponses, UpdateUserErrors, ThrowOnError>({
    security: [{ name: 'x-ni-api-key', type: 'apiKey' }],
    url: '/users/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List workspaces
 *
 * List the workspaces of an organization for administrative operations
 * such as verifying a workspace exists before creating auth-mappings or
 * managing workspace configuration. Only workspaces for which the caller
 * has the <b>workspace:Read</b> permission are returned. Few users are granted
 * this permission; to list workspaces accessible to the current user for
 * non-administrative purposes, use the Auth Service's <b>/niauth/v1/auth</b>
 * route instead.
 *
 */
export const getWorkspaces = <ThrowOnError extends boolean = false>(options?: Options<GetWorkspacesData, ThrowOnError>): RequestResult<GetWorkspacesResponses, GetWorkspacesErrors, ThrowOnError> => (options?.client ?? client).get<GetWorkspacesResponses, GetWorkspacesErrors, ThrowOnError>({
    security: [{ name: 'x-ni-api-key', type: 'apiKey' }],
    url: '/workspaces',
    ...options
});

/**
 * Create a workspace
 *
 * Create a new workspace within the caller's organization to isolate a set
 * of resources (test results, systems, files, etc.) for a specific team,
 * project, or production line. Workspace names must be unique within the
 * organization. After creating the workspace, create auth-mappings to
 * grant access to users.
 *
 */
export const createWorkspace = <ThrowOnError extends boolean = false>(options?: Options<CreateWorkspaceData, ThrowOnError>): RequestResult<CreateWorkspaceResponses, CreateWorkspaceErrors, ThrowOnError> => (options?.client ?? client).post<CreateWorkspaceResponses, CreateWorkspaceErrors, ThrowOnError>({
    security: [{ name: 'x-ni-api-key', type: 'apiKey' }],
    url: '/workspaces',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options?.headers
    }
});

/**
 * Update a workspace
 *
 * Update the name or enabled state of an existing workspace. Rename a
 * workspace when a project name changes, or disable a workspace that
 * should no longer accept new data while preserving its existing resources
 * for historical reference. Disabling a workspace prevents creation of new
 * resources in it but does not hide existing resources from queries.
 *
 */
export const updateWorkspace = <ThrowOnError extends boolean = false>(options: Options<UpdateWorkspaceData, ThrowOnError>): RequestResult<UpdateWorkspaceResponses, UpdateWorkspaceErrors, ThrowOnError> => (options.client ?? client).put<UpdateWorkspaceResponses, UpdateWorkspaceErrors, ThrowOnError>({
    security: [{ name: 'x-ni-api-key', type: 'apiKey' }],
    url: '/workspaces/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List auth-mappings
 *
 * List the authorization mappings configured for the caller's organization
 * to audit which permissions are configured for a given workspace, display
 * the current access configuration in an administration UI, or
 * troubleshoot why a user has or lacks specific permissions. Filter by
 * <b>workspace</b> or <b>type</b> to narrow results.
 *
 */
export const getAuthMappings = <ThrowOnError extends boolean = false>(options?: Options<GetAuthMappingsData, ThrowOnError>): RequestResult<GetAuthMappingsResponses, GetAuthMappingsErrors, ThrowOnError> => (options?.client ?? client).get<GetAuthMappingsResponses, GetAuthMappingsErrors, ThrowOnError>({
    security: [{ name: 'x-ni-api-key', type: 'apiKey' }],
    url: '/auth-mappings',
    ...options
});

/**
 * Create an auth-mapping
 *
 * Create a new authorization mapping that assigns a policy (or policy
 * template bound to a workspace) to users matching a selector criteria.
 * The most common pattern is to specify a <b>policyTemplateId</b> (a role like
 * "Collaborator" or "Data Maintainer") together with a <b>workspace</b> ID so
 * that the system creates a workspace-scoped policy from the template.
 * Use <b>type: "user-id"</b> for individual assignments, or
 * <b>type: "oidc-claim"</b> to provision groups of users based on identity
 * provider claims.
 *
 */
export const createAuthMapping = <ThrowOnError extends boolean = false>(options?: Options<CreateAuthMappingData, ThrowOnError>): RequestResult<CreateAuthMappingResponses, CreateAuthMappingErrors, ThrowOnError> => (options?.client ?? client).post<CreateAuthMappingResponses, CreateAuthMappingErrors, ThrowOnError>({
    security: [{ name: 'x-ni-api-key', type: 'apiKey' }],
    url: '/auth-mappings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options?.headers
    }
});

/**
 * Delete an auth-mapping
 *
 * Permanently delete an authorization mapping to revoke a user's or
 * group's access to a workspace, or to clean up obsolete mappings after
 * decommissioning a workspace. Users previously matched by this mapping
 * will lose the associated policy on their next authentication.
 *
 */
export const deleteAuthMapping = <ThrowOnError extends boolean = false>(options: Options<DeleteAuthMappingData, ThrowOnError>): RequestResult<DeleteAuthMappingResponses, DeleteAuthMappingErrors, ThrowOnError> => (options.client ?? client).delete<DeleteAuthMappingResponses, DeleteAuthMappingErrors, ThrowOnError>({
    security: [{ name: 'x-ni-api-key', type: 'apiKey' }],
    url: '/auth-mappings/{id}',
    ...options
});

/**
 * Update an auth-mapping
 *
 * Update the type, policy, or selector of an existing authorization
 * mapping to change a user's role in a workspace, re-target a mapping to
 * a different group after an organizational restructure, or correct a
 * selector value.
 *
 */
export const updateAuthMapping = <ThrowOnError extends boolean = false>(options: Options<UpdateAuthMappingData, ThrowOnError>): RequestResult<UpdateAuthMappingResponses, UpdateAuthMappingErrors, ThrowOnError> => (options.client ?? client).put<UpdateAuthMappingResponses, UpdateAuthMappingErrors, ThrowOnError>({
    security: [{ name: 'x-ni-api-key', type: 'apiKey' }],
    url: '/auth-mappings/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get an organization
 *
 * Returns the organization with the given name, including its ID, list of
 * workspaces, and owner account. Requires a whitelisted API key because
 * this endpoint is used by internal services during system bootstrap or
 * cross-service coordination and is not available to regular API keys.
 *
 */
export const getOrg = <ThrowOnError extends boolean = false>(options: Options<GetOrgData, ThrowOnError>): RequestResult<GetOrgResponses, GetOrgErrors, ThrowOnError> => (options.client ?? client).get<GetOrgResponses, GetOrgErrors, ThrowOnError>({
    security: [{ name: 'x-ni-api-key', type: 'apiKey' }],
    url: '/orgs/{name}',
    ...options
});

/**
 * Open a WebSocket session
 *
 * Opens a persistent WebSocket connection for receiving real-time
 * notifications when new workspaces are created, allowing UIs to refresh
 * workspace lists or services to automatically provision default resources.
 * After opening the connection, send a
 * <b>UserAsyncWorkspacesSubscribeRequest</b> message to subscribe to workspace
 * creation events. The server sends <b>UserWorkspacesCreatedRoutedMessage</b>
 * messages when new workspaces are created. Refer to the *Models* section
 * for message schemas.
 *
 */
export const openWebSocketSession = <ThrowOnError extends boolean = false>(options: Options<OpenWebSocketSessionData, ThrowOnError>): RequestResult<OpenWebSocketSessionResponses, OpenWebSocketSessionErrors, ThrowOnError> => (options.client ?? client).get<OpenWebSocketSessionResponses, OpenWebSocketSessionErrors, ThrowOnError>({
    security: [{ name: 'x-ni-api-key', type: 'apiKey' }],
    url: '/websocket',
    ...options
});
