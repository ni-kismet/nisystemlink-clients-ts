// This file is auto-generated by @hey-api/openapi-ts

import type { Client, Options as Options2, TDataShape } from './client';
import { client } from './client.gen';
import type { CreateTableData, CreateTableErrors, CreateTableResponses, DeleteTableData, DeleteTableErrors, DeleteTableResponses, DeleteTablesData, DeleteTablesErrors, DeleteTablesResponses, ExportTableDataData, ExportTableDataErrors, ExportTableDataResponses, GetTableData, GetTableDataData, GetTableDataErrors, GetTableDataResponses, GetTableErrors, GetTableResponses, GetTablesData, GetTablesErrors, GetTablesResponses, ModifyTableData, ModifyTableErrors, ModifyTableResponses, ModifyTablesData, ModifyTablesErrors, ModifyTablesResponses, PostTableDataData, PostTableDataErrors, PostTableDataResponses, QueryDecimatedTableDataData, QueryDecimatedTableDataErrors, QueryDecimatedTableDataResponses, QueryTableDataData, QueryTableDataErrors, QueryTableDataResponses, QueryTablesData, QueryTablesErrors, QueryTablesResponses, RootEndpointData, RootEndpointResponses, RootEndpointWithVersionData, RootEndpointWithVersionErrors, RootEndpointWithVersionResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean> = Options2<TData, ThrowOnError> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: Record<string, unknown>;
};

/**
 * Get rows of data from the specified table
 *
 * Reads raw data from the table identified by its ID. For more advanced filtering and ordering, use the `POST /v1/tables/{id}/query-data` route.
 */
export const getTableData = <ThrowOnError extends boolean = false>(options: Options<GetTableDataData, ThrowOnError>) => (options.client ?? client).get<GetTableDataResponses, GetTableDataErrors, ThrowOnError>({
    security: [{ name: 'X-NI-API-KEY', type: 'apiKey' }],
    url: '/nidataframe/v1/tables/{id}/data',
    ...options
});

/**
 * Append one or more rows of data to the table identified by its ID
 *
 * Multiple data formats are supported, distinguished by the `Content-Type` header
 * of the request:
 *
 *
 * - `application/json`: Each row of data must contain a value for each non-nullable column
 * in the table, in a format that can be converted to the column's datatype. Any nullable
 * columns not included in `data` will receive a null value for each appended row. `endOfData`
 * is provided in the request body.
 *
 *
 * - `application/vnd.apache.arrow.stream`: [Apache Arrow IPC Streaming](https://www.ni.com/r/arrow-ipc-format)
 * binary format. The data must contain a column for each non-nullable column in the table
 * using data types compatible with the column's datatype. Any nullable columns not included
 * in the request will receive a null value for each appended row. Timestamp values are
 * truncated to milliseconds, even when using nanosecond or microsecond units. `endOfData`
 * is provided as a query parameter. Added in version 2 of the `writeData` operation.
 */
export const postTableData = <ThrowOnError extends boolean = false>(options: Options<PostTableDataData, ThrowOnError>) => (options.client ?? client).post<PostTableDataResponses, PostTableDataErrors, ThrowOnError>({
    security: [{ name: 'X-NI-API-KEY', type: 'apiKey' }],
    url: '/nidataframe/v1/tables/{id}/data',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Query for rows of data with ordering and filtering
 *
 * Reads rows of data that match a filter from the table identified by its ID.
 *
 *
 * Use the `columns` property to return a subset of each row.
 *
 *
 * Use the `filters` property to filter the returned rows.
 *
 *
 * Use the `orderBy` and `filters` properties to order and filter the returned rows.
 *
 *
 * Use the `take` and `continuationToken` properties to return paged responses.
 * As of version 4 of the `readData` operation, the maximum `take` value is 10,000.
 *
 *
 * For requests that don't require filtering, use the `GET /v1/tables/{id}/data` route.
 */
export const queryTableData = <ThrowOnError extends boolean = false>(options: Options<QueryTableDataData, ThrowOnError>) => (options.client ?? client).post<QueryTableDataResponses, QueryTableDataErrors, ThrowOnError>({
    security: [{ name: 'X-NI-API-KEY', type: 'apiKey' }],
    url: '/nidataframe/v1/tables/{id}/query-data',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Export data with sorting and filtering
 *
 * Exports rows of data that match a filter from the table identified by its ID.
 *
 *
 * Use the `columns` property to return a subset of each row.
 *
 *
 * Use the `orderBy` and `filters` properties to order and filter the returned rows.
 *
 *
 * Use the `take` property to limit the number of rows returned.
 *
 *
 * Use the `responseFormat` property to select the format of the exported content. The only response format currently supported is `CSV`.
 *
 *
 * Added in version 2 of the `readData` operation. The `take` property was added in version 3 of the `readData` operation.
 */
export const exportTableData = <ThrowOnError extends boolean = false>(options: Options<ExportTableDataData, ThrowOnError>) => (options.client ?? client).post<ExportTableDataResponses, ExportTableDataErrors, ThrowOnError>({
    security: [{ name: 'X-NI-API-KEY', type: 'apiKey' }],
    url: '/nidataframe/v1/tables/{id}/export-data',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Query for rows of decimated data with filtering
 *
 * Reads rows of data that match a filter from the table identified by its ID.
 *
 *
 * Use the `columns` property to return a subset of each row.
 *
 *
 * Use the `filters` property to filter the returned rows.
 *
 *
 * Use the `decimation` property to specify parameters on how data will be decimated.
 *
 *
 * For more requests that don't require data decimation, use the `POST /v1/tables/{id}/query-data` route.
 */
export const queryDecimatedTableData = <ThrowOnError extends boolean = false>(options: Options<QueryDecimatedTableDataData, ThrowOnError>) => (options.client ?? client).post<QueryDecimatedTableDataResponses, QueryDecimatedTableDataErrors, ThrowOnError>({
    security: [{ name: 'X-NI-API-KEY', type: 'apiKey' }],
    url: '/nidataframe/v1/tables/{id}/query-decimated-data',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List available tables
 *
 * Lists available tables on the SystemLink DataFrame service.
 *
 *
 * Use the `take` and `continuationToken` parameters to return paged responses.
 *
 *
 * The `orderBy` and `orderByDescending` parameters can be used to manage sorting the list by metadata objects.
 */
export const getTables = <ThrowOnError extends boolean = false>(options?: Options<GetTablesData, ThrowOnError>) => (options?.client ?? client).get<GetTablesResponses, GetTablesErrors, ThrowOnError>({
    security: [{ name: 'X-NI-API-KEY', type: 'apiKey' }],
    url: '/nidataframe/v1/tables',
    ...options
});

/**
 * Create a new table
 *
 * Create a new table with the provided metadata and column definitions.
 */
export const createTable = <ThrowOnError extends boolean = false>(options?: Options<CreateTableData, ThrowOnError>) => (options?.client ?? client).post<CreateTableResponses, CreateTableErrors, ThrowOnError>({
    security: [{ name: 'X-NI-API-KEY', type: 'apiKey' }],
    url: '/nidataframe/v1/tables',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options?.headers
    }
});

/**
 * Query available tables
 *
 * Queries available tables on the SystemLink DataFrame service and returns the table metadata.
 *
 *
 * Use the `take` and `continuationToken` properties to return paged responses.
 *
 *
 * The `orderBy` and `orderByDescending` properties can be used to manage sorting the list by metadata objects.
 *
 *
 */
export const queryTables = <ThrowOnError extends boolean = false>(options?: Options<QueryTablesData, ThrowOnError>) => (options?.client ?? client).post<QueryTablesResponses, QueryTablesErrors, ThrowOnError>({
    security: [{ name: 'X-NI-API-KEY', type: 'apiKey' }],
    url: '/nidataframe/v1/query-tables',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options?.headers
    }
});

/**
 * Delete a table
 *
 * Deletes the table identified by its ID.
 */
export const deleteTable = <ThrowOnError extends boolean = false>(options: Options<DeleteTableData, ThrowOnError>) => (options.client ?? client).delete<DeleteTableResponses, DeleteTableErrors, ThrowOnError>({
    security: [{ name: 'X-NI-API-KEY', type: 'apiKey' }],
    url: '/nidataframe/v1/tables/{id}',
    ...options
});

/**
 * Get table metadata and column info
 *
 * Retrieves the metadata and column information for a single table identified by its ID. This route does not return row data.
 */
export const getTable = <ThrowOnError extends boolean = false>(options: Options<GetTableData, ThrowOnError>) => (options.client ?? client).get<GetTableResponses, GetTableErrors, ThrowOnError>({
    security: [{ name: 'X-NI-API-KEY', type: 'apiKey' }],
    url: '/nidataframe/v1/tables/{id}',
    ...options
});

/**
 * Modify metadata of table
 *
 * Modify the properties associated with the table or columns of the table identified by its ID.
 */
export const modifyTable = <ThrowOnError extends boolean = false>(options: Options<ModifyTableData, ThrowOnError>) => (options.client ?? client).patch<ModifyTableResponses, ModifyTableErrors, ThrowOnError>({
    security: [{ name: 'X-NI-API-KEY', type: 'apiKey' }],
    url: '/nidataframe/v1/tables/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete one or more tables
 *
 * Deletes tables identified by their IDs.
 */
export const deleteTables = <ThrowOnError extends boolean = false>(options?: Options<DeleteTablesData, ThrowOnError>) => (options?.client ?? client).post<DeleteTablesResponses, DeleteTablesErrors, ThrowOnError>({
    security: [{ name: 'X-NI-API-KEY', type: 'apiKey' }],
    url: '/nidataframe/v1/delete-tables',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options?.headers
    }
});

/**
 * Modify metadata of one or more tables
 *
 * Modify the properties associated with the tables identified by their IDs.
 */
export const modifyTables = <ThrowOnError extends boolean = false>(options?: Options<ModifyTablesData, ThrowOnError>) => (options?.client ?? client).post<ModifyTablesResponses, ModifyTablesErrors, ThrowOnError>({
    security: [{ name: 'X-NI-API-KEY', type: 'apiKey' }],
    url: '/nidataframe/v1/modify-tables',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options?.headers
    }
});

/**
 * API information
 *
 * Returns information about API versions and available operations.
 */
export const rootEndpoint = <ThrowOnError extends boolean = false>(options?: Options<RootEndpointData, ThrowOnError>) => (options?.client ?? client).get<RootEndpointResponses, unknown, ThrowOnError>({ url: '/nidataframe', ...options });

/**
 * API version information
 *
 * Returns available operations for a single version of the API.
 */
export const rootEndpointWithVersion = <ThrowOnError extends boolean = false>(options: Options<RootEndpointWithVersionData, ThrowOnError>) => (options.client ?? client).get<RootEndpointWithVersionResponses, RootEndpointWithVersionErrors, ThrowOnError>({ url: '/nidataframe/{version}', ...options });
